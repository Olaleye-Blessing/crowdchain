"use client";

import { IAddress } from "@/interfaces/address";

// TODO: Display total donations with total amount
export default function Overview({ account }: { account: IAddress }) {
  return <section>Coming Soon...</section>;
}
